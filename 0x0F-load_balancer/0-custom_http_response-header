#!/usr/bin/env bash
#laaaazzzzy

sudo apt -y update
sudo apt -y install nginx
sudo service nginx start
myHostName="$(cat /etc/hostname)"
forChange="http {\n\tadd_header X-Server-By $myHostName;\n"
sed -i "s/http {/$forChange/" /etc/nginx/nginx.conf
sudo service nginx restart
